name: Vue Package CI/CD

on:
  push:
    branches: [main, master, "claude/**"]
    paths:
      - "packages/vue/**"
      - "packages/core/**"
      - ".github/workflows/ci-vue.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "packages/vue/**"
      - "packages/core/**"
      - ".github/workflows/ci-vue.yml"
  release:
    types: [published]

jobs:
  ci:
    name: Test & Build
    runs-on: ubuntu-latest
    if: >
      github.event_name != 'release' ||
      startsWith(github.event.release.tag_name, 'vue@v')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies (monorepo root)
        run: npm ci

      - name: Run tests (Vitest)
        run: npm test
        working-directory: packages/vue

      - name: Build (Vite)
        run: npm run build
        working-directory: packages/vue

      - name: Verify dist output
        run: |
          echo "=== dist/ contents ==="
          ls -lh dist/
          echo "=== ES module entry (first 5 lines) ==="
          head -5 dist/index.es.js
          echo "=== CJS entry (first 5 lines) ==="
          head -5 dist/index.cjs.js
        working-directory: packages/vue

  publish:
    name: Publish to npm
    needs: ci
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'release' &&
      github.event.action == 'published' &&
      startsWith(github.event.release.tag_name, 'vue@v')

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org
          cache: npm

      - name: Install dependencies (monorepo root)
        run: npm ci

      - name: Build (Vite)
        run: npm run build
        working-directory: packages/vue

      - name: Publish @allansli/vue-barcode-febraban
        run: npm publish --access public --provenance
        working-directory: packages/vue
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
